{
  "manifest_version": 3,
  "name": "Media Center Keys",
  "version": "1.1",
  "description": "Ctrl+Q closes the kiosk browser the way it closes VLC; F puts the player full screen; W watches the page's film in Aurora's own player.",
  "background": { "service_worker": "background.js" },
  "host_permissions": ["http://127.0.0.1:8765/*"],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["quit.js", "fullscreen.js", "watch.js"],
      "run_at": "document_start",
      "all_frames": true,
      "match_origin_as_fallback": true
    }
  ],
  "commands": {
    "quit-browser": {
      "suggested_key": { "default": "Ctrl+Q" },
      "description": "Close the browser and return to the media center"
    }
  }
}
