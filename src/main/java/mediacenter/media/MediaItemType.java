package mediacenter.media;

/** What a browsable entry is. */
public enum MediaItemType {

    /** A directory the user can enter. */
    DIRECTORY,

    /** A playable video file. */
    VIDEO,

    /** A photograph, shown full screen rather than handed to the player. */
    IMAGE
}
